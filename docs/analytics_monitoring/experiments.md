# Эксперименты и A/B Тестирование (Концепция) 🧪

Инфраструктура для проверки гипотез о влиянии различных AI-моделей и промптов на пользовательский опыт. 

### Философия: Детерминизм и Консистентность
В отличие от классического A/B тестирования кнопок, тестирование ИИ требует особого тщательности:
- **Sticky Experience**: Решение о назначении варианта (`Variant A` или `Variant B`) принимается на основе ID сессии. Это гарантирует, что на протяжении всего диалога пользователь работает с одной и той же моделью, и его не "кидает" между разными логиками ответов.
- **Traceability**: Каждое взаимодействие с ИИ метится ID эксперимента. Это позволяет нам понимать, что именно повлияло на итоговый выбор пользователя — конкретный промпт или конкретная модель.

### Архитектура реализации

1.  **Bucketing (Распределение)**: 
    Используется детерминированное хеширование `session_id` + `experiment_id`. Это исключает необходимость хранить распределение в базе данных и позволяет мгновенно определять вариант пользователя без лишних сетевых запросов.
    
2.  **Overrides (Переопределения)**:
    `ExperimentService` динамически подменяет параметры в `AIReasoningService`. Мы можем менять «на лету» системные промпты, модели или параметры температуры для конкретной группы пользователей.

3.  **Аналитика**:
    Данные из `LLMLog` агрегируются через GraphQL, позволяя накладывать технические метрики (стоимость, токены) на продуктовые (лайки, покупки).

---

📖 **Техническое описание эндпоинтов и примеры запросов доступны в [API Reference](../api/analytics/experiments.md).**
