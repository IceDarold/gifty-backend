# Бизнес-метрики и KPI 📈

Бизнес-аналитика Gifty сфокусирована на эффективности воронки и удовлетворенности пользователей. Мы используем PostHog как основное хранилище поведенческих данных, агрегируя их через наш GraphQL API.

### Основные показатели (Primary Metrics)

1.  **Completion Rate (Конверсия квиза)**:
    Процент пользователей, которые начали квиз и дошли до стадии получения рекомендаций. Это главный индикатор качества нашего UX.
    
2.  **Gift CTR (Click-Through Rate)**:
    Отношение кликов по товарам к общему числу показов рекомендаций. Если CTR падает — значит, наши алгоритмы поиска стали менее релевантными.

3.  **Active Sessions (DAU/MAU)**:
    Количество уникальных сессий. Позволяет оценивать масштаб использования платформы.

### Анализ воронки (Funnel Analysis)

Мы разбиваем путь пользователя на микро-шаги, чтобы находить "узкие места":
- `quiz_started` ➡ `quiz_step_X` ➡ `quiz_completed` ➡ `results_shown` ➡ `item_clicked` ➡ `purchase_external`.

### Почему это важно?

- **Продуктовые решения**: Мы видим, на каком именно вопросе квиза пользователи «отваливаются» чаще всего.
- **Оценка ИИ**: Мы можем сравнить CTR рекомендаций, сгенерированных разными моделями (через A/B тесты).

---

📖 **Техническое описание эндпоинтов для получения KPI доступно в [API Reference -> Business Metrics](../api/analytics/kpi.md).**
