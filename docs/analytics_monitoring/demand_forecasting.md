# Прогноз спроса (Demand Forecasting) 📈

Инструмент трансформации «неудовлетворенного спроса» пользователей в приоритеты для парсинга и наполнения каталога.

### Проблема: Дисбаланс каталога
Часто ИИ генерирует отличные идеи (например, «Настольные игры»), но в нашей базе данных товаров этой категории оказывается критически мало. Это приводит к тому, что пользователь видит пустые выдачи или нерелевантные замены, что резко снижает конверсию в покупку.

### Механика работы

1.  **Детекция дефицита**:
    При каждом поиске ИИ в `SearchLog` записывается поле `predicted_category` (категория, к которой относится запрос) и `results_count` (сколько товаров найдено в базе). Все запросы, вернувшие менее 3 результатов, помечаются как «дефицитные».

2.  **Классификация запросов**:
    Разрозненные поисковые запросы группируются в глобальные категории нашего каталога через ИИ-классификатор (track title).

3.  **Аналитика и Deficit Index**:
    Рассчитывается коэффициент дефицита. Формула: `searches_count / (avg_results + 0.5)`. Чем выше индекс, тем критичнее нехватка товаров в этой категории.

### Применение: Умный парсинг

Результаты этого отчета используются для формирования **Parsing Priority List**. Наши внешние воркеры и парсеры получают задания на основе данных о реальном спросе:
- Если категория получает высокий `deficitScore`, парсинг этой категории на маркетплейсах (Detmir и др.) ускоряется.
- Мы тратим бюджет на прокси и серверы парсинга только там, где это принесет реальные продажи.

---

📖 **Техническое описание GraphQL запроса доступно в [API Reference -> Demand Forecasting](../api/analytics/demand_forecasting.md).**
