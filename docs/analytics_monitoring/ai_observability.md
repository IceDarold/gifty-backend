# AI Observability (Наблюдаемость ИИ) 🧠

Поскольку ИИ является «сердцем» Gifty, мы уделяем приоритетное внимание глубокому мониторингу каждого вызова LLM. Это позволяет нам балансировать между качеством ответов, скоростью их генерации и стоимостью инфраструктуры.

### Зачем мы это отслеживаем?

1.  **Контроль затрат (Cost Control)**: 
    Разные модели имеют разную стоимость (напр. Claude Opus в десятки раз дороже Claude Haiku). Система автоматически считает стоимость каждого запроса на основе актуальных цен провайдеров, позволяя нам видеть "экономику" каждой сессии.

2.  **Оптимизация задержек (Latency Analysis)**: 
    Пользователи не любят ждать. Мы отслеживаем `latency_ms` для каждого этапа генерации, чтобы понимать, на каком этапе (генерация гипотез или ранжирование) происходят замедления.

3.  **Анализ токенов (Token Usage)**: 
    Мониторинг объема контекста (Prompt Tokens) и ответов (Completion Tokens) помогает нам оптимизировать промпты, убирая из них лишнее и экономя ресурсы.

4.  **Распределение провайдеров**: 
    Мы отслеживаем, как нагрузка распределяется между OpenAI, Anthropic, Gemini и Groq, чтобы избежать зависимости от одного поставщика.

### Инструменты анализа

- **Сквозное логирование**: Каждый лог ИИ связан с ID сессии. Мы можем «отмотать» историю и посмотреть, какой именно промпт привел к конкретному ответу.
- **Мониторинг качества (Hypothesis Analysis)**: Мы связываем сгенерированную гипотезу с реальными результатами поиска, чтобы видеть "галлюцинации" — когда ИИ придумывает подарок, которого не существует в реальности.

---

📖 **Техническое описание эндпоинтов `aiUsage` и `llmLogs` доступно в [API Reference -> AI Observability](../api/analytics/ai_observability.md).**
