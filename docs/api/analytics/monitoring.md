# Мониторинг и Здоровье 🛠️

Технические метрики системы и статус парсеров.

### Техническое состояние (`technical`)

Данные из Prometheus и логи ошибок из Loki.

**Query:**
```graphql
query {
  technical {
    apiHealth
    requestsPerMinute
    errorRate5xx
    lastErrors
    lastUpdated
  }
}
```

---

### Статус Парсеров (`scraping`)

Мониторинг процесса сбора данных.

**Query:**
```graphql
query {
  scraping {
    activeSources
    unmappedCategories
    totalScrapedItems
    ingestionErrors
    spiders
  }
}
```

**Описание:**
- `unmappedCategories`: Количество категорий внешних сайтов, которые еще не привязаны к внутренней структуре Gifty (требуют внимания ИИ или админа).
- `spiders`: Детальная статистика по каждому активному пауку (Scrapy).
