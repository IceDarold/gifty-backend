# Demand Forecasting (API) 📈

Техническое описание GraphQL эндпоинта для анализа дефицита товаров в категориях.

### GraphQL API: `demandForecast`

Запрос агрегирует исторические данные поисков и возвращает список категорий с наибольшим дефицитом.

**Query:**
```graphql
query {
  demandForecast(days: 7) {
    totalSearchesAnalyzed    # Общее кол-во проанализированных поисков
    lastUpdated              # Время последнего обновления
    topDeficitCategories {
      categoryName           # Название категории ("Настольные игры")
      searchesCount          # Сколько раз люди искали эту тему
      avgResultsFound        # Среднее кол-во товаров в выдаче (если < 3 — это дефицит)
      zeroResultRate         # % поисков, которые вообще ничего не нашли
      deficitScore           # Главный индекс для приоритизации (от большего к меньшему)
    }
  }
}
```

---

📖 **Подробное описание концепции и применения в бизнесе доступно в [Analytics & Monitoring -> Demand Forecasting](../../analytics_monitoring/demand_forecasting.md).**
