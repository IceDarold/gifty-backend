# Охват каталога 📦

Анализ того, насколько товары в базе соответствуют поисковым запросам от ИИ.

### Обзор охвата (`catalogCoverage`)

Позволяет узнать Hit Rate (процент успешных поисков) и выявить "дыры" в каталоге.

**Query:**
```graphql
query GetCoverage($days: Int!) {
  catalogCoverage(days: $days) {
    totalSearches
    hitRate
    avgResultsPerSearch
    topCatalogGaps {
      query
      misses
    }
  }
}
```

**Описание полей:**
- `hitRate`: Процент запросов, по которым был найден хотя бы один товар.
- `topCatalogGaps`: Список запросов, которые чаще всего выдавали 0 результатов (сигнал для добавления новых источников парсинга).
