# Анализ Покрытия Каталога 📦

Инструменты для оценки того, насколько хорошо ассортимент товаров соответствует запросам пользователей и нейросети.

### 1. Текущее Покрытие (`/catalog/coverage`)
Общий срез эффективности поиска за период.

*   **URL**: `/analytics/catalog/coverage`
*   **Метод**: `GET`
*   **Параметры**:
    - `days` (query, optional): Глубина анализа в днях (default: 7).
*   **Поля ответа**:
    - `hit_rate`: % запросов с хотя бы одним результатом.
    - `avg_results_per_search`: Среднее кол-во найденных товаров.
    - `top_catalog_gaps`: Самые частые запросы с 0 результатов.

---

### 2. Тренды Покрытия (`/catalog/coverage/trends`)
Временные ряды показателей наполненности каталога. Позволяют оценить влияние контентных правок.

*   **URL**: `/analytics/catalog/coverage/trends`
*   **Метод**: `GET`

---

### 3. Сегментация Покрытия (`/catalog/coverage/segments`)
Анализ «узких мест» по категориям, моделям или бюджету.

*   **URL**: `/analytics/catalog/coverage/segments`
*   **Параметры**:
    - `group_by`: `budget`, `model` или `track`.

---

### 4. Детализация запроса (`/catalog/coverage/drilldown`)
Глубокое исследование конкретного поискового паттерна для выявления причин отсутствия товаров.

*   **URL**: `/analytics/catalog/coverage/drilldown?query=...`
*   **Зачем**: Понять контекст (модели, сессии), в котором возник провал поиска.
