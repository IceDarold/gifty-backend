# Бизнес-метрики и KPI 📊

Все основные показатели теперь доступны через один GraphQL запрос.

### Основные метрики (`stats`)

Запрос возвращает DAU, процент завершения квиза и CTR подарков.

**Query:**
```graphql
query {
  stats {
    dau
    quizCompletionRate
    giftCtr
    totalSessions
    lastUpdated
  }
}
```

---

### Тренды и Графики (`trends`)

Возвращает временные ряды для построения графиков.

**Query:**
```graphql
query GetTrends($days: Int!) {
  trends(days: $days) {
    dates
    dauTrend
    quizStarts
    lastUpdated
  }
}
```

---

### Воронка Конверсии (`funnel`)

Данные о прохождении пользователем основных этапов за последние 30 дней.

**Query:**
```graphql
query {
  funnel {
    name
    count
    conversionRate
  }
}
```
