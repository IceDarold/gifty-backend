# AI Observability 🧠

Система отслеживания работы ИИ-моделей в реальном времени. Позволяет контролировать расходы, качество ответов и задержки (latency).

### Статистика использования ИИ (`aiUsage`)

Агрегированные данные по всем LLM-запросам за указанный период.

**Query:**
```graphql
query {
  aiUsage(days: 7) {
    totalTokens
    totalCostUsd
    avgLatencyMs
    requestsCount
    providerDistribution
    lastUpdated
  }
}
```

**Описание полей:**
- `totalTokens`: Суммарное количество токенов (input + output).
- `totalCostUsd`: Оценочная стоимость запросов в USD.
- `avgLatencyMs`: Среднее время ответа модели.
- `providerDistribution`: Распределение запросов по провайдерам (напр. {"anthropic": 150, "gemini": 50}).

---

### Логи вызовов LLM (`llmLogs`)

Детальный список последних взаимодействий с языковыми моделями.

**Query:**
```graphql
query {
  llmLogs(limit: 10) {
    id
    provider
    model
    callType
    latencyMs
    totalTokens
    costUsd
    createdAt
  }
}
```

**Типы вызовов (`callType`):**
- `normalize_topics`: Стандартизация интересов пользователя.
- `classify_topic`: Определение глубины темы.
- `generate_hypotheses`: Генерация идей подарков.
- `probe_exploration`: Уточняющие вопросы.

---

### Анализ качества (`hypothesisDetails`)

Вы можете проанализировать конкретную гипотезу, чтобы увидеть, какие поисковые запросы сгенерировал ИИ и какие товары были найдены.

**Query:**
```graphql
query {
  hypothesisDetails(id: "UUID-ГИПОТЕЗЫ") {
    title
    track
    reaction
    searchQueries
    totalResultsFound
    products {
      giftId
      score
      rank
      clicked
    }
  }
}
```
