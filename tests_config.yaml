# Configuration for test execution
# Set to false to skip the entire group of tests

test_groups:
  recommendations: true
  routes: true
  auth: true
  integrations: true
  security: true
  ai_intelligence: false  # Expensive tests set to false by default
